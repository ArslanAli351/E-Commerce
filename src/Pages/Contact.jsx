


export default function Contact(){
    return(<>
    <h1 className=" qs  :bg-primary">Contact page</h1>
    {/* {searchResult?.map((item) => {
        let value = item.reviews.map((reviews) => {
          return reviews.comment;
        }); */}
     {/* <MoreProducts 
          key={item.id}
          image={item.thumbnail}
          name={item.brand}
          price={item.price}
          discount={item.discountPercentage}
          // reviews={value}
          rating={item.rating}
          /> */}
    </>)
}